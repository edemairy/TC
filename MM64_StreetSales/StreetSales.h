#include <vector>
#include <string>
using namespace std;

class StreetSales {
  public:
    vector <string> dayTrade(vector <int> visitedHouses);
    int init(vector <string> districtMap, vector <int> warehousePrices, int C, int S);
};
