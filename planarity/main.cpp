#include <iostream>
using namespace std;

int main(int argc, char** argv) {
  V = int(cint.readLine())
  N = int(cint.readLine())
/*  for (i=0; i<N; i++)
      edges[i] = int(readLine())
  int[] coords = untangle(V,edges)
  for (i=0; i<coords.length(); i++)
      printLine(coords[i])
  flush(stdout)*/
}
