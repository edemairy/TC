#include <iostream>
#include <fstream>
using namespace std;

int main(int argc, char** argv) {
  ifstream is( cin );
  int V, N;
  is >> V >> N;
/* int V = int(cin.readLine())
  int N = int(cin.readLine())
  for (i=0; i<N; i++)
      edges[i] = int(readLine())
  int[] coords = untangle(V,edges)
  for (i=0; i<coords.length(); i++)
      printLine(coords[i])
  flush(stdout)*/
}
